; Set process limit de QTP
;
; Description: Sert à initialiser les paramètres globales de QTP.
;
; Application: Doit être introduit au début du source.
;
;
SET DEFAULT

@IF NOT HPMPEXL
SET PROCESS NOLIMIT
@ELSE
SET PROCESS LIMIT 100000
@ENDIF
;
