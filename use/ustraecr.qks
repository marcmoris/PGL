; Transaction QUERY/UPDATE pour les écrans maîtres.
;
; Le but est séparer les transactions par unité de traitement. Cela évite
; d'hériter des transactions de écran supérieur.
;
;
@IF VAXVMS OR UNIX
SET LIST TRANSACTION ; Pour voir les transactions utilisées

;TRANSACTION QUERY  READ ONLY  READ COMMITTED   COMMIT ON MODE

;TRANSACTION UPDATE READ WRITE REPEATABLE READ COMMIT ON UPDATE
@ENDIF
