set proc nolim input nolim
request r2
acc armou_detail
subfile wbck_armou_detail keep include armou_detail
request r2
acc armou_detail link ciecle of armou_detail   , &
                      moucle of armou_detail     &
                   to ciecle                   , &
                      moucle of armouvement opt
select if not record armouvement exists
output armou_detail delete
